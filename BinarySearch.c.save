#include <stdio.h>

int main()
{


	return 0;
}

int BinarySearch(int a[], searchElemensearchElementt,low, high)
{
	int mid = (low + high)/2;

	while(low <= high)
	{
		if(a[mid] == searchElement)
		{
			return mid;
		} else if(a[mid] < )
	}


}
